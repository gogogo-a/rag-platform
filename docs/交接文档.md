# AI 交接文档：Qdrant 向量库改造

## 接手摘要

这个项目已经从 Milvus 全面切到 Qdrant。后续 AI 或开发者接手时，不要再恢复 Milvus，也不要再引入 `pymilvus`、`langchain-milvus` 之类依赖。

接手时按以下结论执行：

- 向量库统一使用 Qdrant。
- 文档向量写入、检索、删除走 Qdrant；列表和详情的 `chunk_count` 优先读 MongoDB 已保存的 `extra_data.chunks_count`。
- QA 相似问答缓存也走 Qdrant。
- MCP 知识库搜索工具只调用 Qdrant。
- MCP 自定义工具默认全部启用，不再受 `ENABLE_EXTRA_MCP_TOOLS` 限制。
- 后续字段统一使用 `vector_id`，旧的 `milvus_id` 只作为兼容读取，不应继续新增。
- 启动时只需要 MongoDB、Redis、Qdrant、MCP 等服务，不再需要 Milvus。
- 当前 Qdrant 部署在服务器上，MongoDB 也部署在服务器上；本地没有对应服务时，不代表服务器环境不可用。
- 旧 Milvus 数据不做自动迁移，如果需要旧数据，应走单独快照导入 Qdrant。
- 页面和接口文案不要出现 Milvus，统一写“向量库”或 “Qdrant”。
- 不要恢复 Milvus 文件、Docker 配置或工具限制开关。
- 文档详情正文以 MongoDB `documents.content` 为准，Qdrant 只负责检索和向量数据。

## 当前目标

本项目已从 Milvus 向量库切换为 Qdrant。后续接手时，默认不要再恢复 Milvus，也不要再引入 `pymilvus` 或 `langchain-milvus`。

当前向量相关目标：

- 文档向量写入、检索、删除走 Qdrant。
- 文档列表和详情不要实时逐条扫描 Qdrant 统计分块数。
- QA 缓存的向量写入、相似问题检索和删除全部走 Qdrant。
- MCP 自定义工具默认全部启用。
- 用户可见页面和接口文案不出现 Milvus。

## 关键文件

- `internal/db/qdrant.py`
  - Qdrant 统一客户端封装。
  - 提供 collection 初始化、文档向量 upsert、QA 缓存 upsert、检索、按文档删除、计数、scroll 等方法。

- `main.py`
  - 启动时会初始化 Qdrant 文档集合和 QA 缓存集合。
  - 启动流程不再连接 Milvus。

- `internal/document_client/document_processor.py`
  - 文档解析后生成 embedding，并写入 Qdrant。
  - 处理完成后把完整解析正文回写到 MongoDB `documents.content`。
  - 删除任务按 `document_uuid` 删除 Qdrant 向量。

- `internal/service/orm/document_sever.py`
  - 文档详情和列表的 `chunk_count` 优先读取 MongoDB 里保存的分块数量。
  - 上传接口只保存文件、创建记录、提交后台任务，不在请求内同步解析全文。
  - 删除文档时同步删除 Qdrant 向量。

- `pkg/agent_tools_mcp/knowledge_search_mcp.py`
  - MCP 知识库检索直接调用 Qdrant。
  - 普通用户仍过滤 `permission=1` 的文档。

- `internal/service/ai/thought_chain_store.py`
  - QA 缓存写入 Qdrant。
  - 使用 `vector_id` 记录向量 ID。

- `internal/service/ai/similar_qa_cache.py`
  - 相似问答检索和缓存删除走 Qdrant。

- `internal/service/orm/qa_cache_service.py`
  - QA 缓存管理以 MongoDB 的 `ThoughtChainModel` 为主，Qdrant 只负责向量检索和删除。

- `internal/service/visualization/document_3d_service.py`
  - 3D 可视化从 Qdrant scroll 读取向量。

- `pkg/agent_tools_mcp/mcp_config.py`
  - MCP 工具默认全部加载，不再使用 `ENABLE_EXTRA_MCP_TOOLS`。

## 配置项

当前部署环境：

- Qdrant 在服务器上。
- MongoDB 在服务器上。
- 后端应通过环境变量连接服务器上的 Qdrant 和 MongoDB。
- 本地开发机如果没有启动 Qdrant 或 MongoDB，不应据此判断服务器连接异常。

`.env` / `env_template.txt` 中向量库配置为：

```env
QDRANT_HOST=localhost
QDRANT_PORT=6333
QDRANT_COLLECTION_NAME=documents
QDRANT_QA_COLLECTION_NAME=qa_cache
VECTOR_DIMENSION=1024
```

依赖中应保留：

```txt
qdrant-client
```

依赖中不应出现：

```txt
pymilvus
langchain-milvus
```

## 数据流

文档上传：

1. API 保存文件和 MongoDB 文档记录。
2. API 提交后台处理任务并立即返回处理中。
3. `document_processor` 解析文本并分块。
4. `embedding_service` 生成向量。
5. `qdrant_client.upsert_documents()` 写入 Qdrant。
6. 文档状态、正文、分块数量更新到 MongoDB。

知识库检索：

1. MCP 工具收到 query。
2. `embedding_service.encode_query()` 生成 query embedding。
3. `qdrant_client.search_documents()` 检索 Qdrant。
4. 根据 `permission` 过滤结果。
5. 返回 context 和引用文档列表。

QA 缓存：

1. 思维链写入 MongoDB。
2. 需要缓存时，问题 embedding 写入 `QDRANT_QA_COLLECTION_NAME`。
3. 相似问题检索从 QA collection 查询。
4. 删除缓存时同步清空 `vector_id` 并删除 Qdrant point。

## 验证命令

改动后至少运行：

```bash
rg -n "milvus|Milvus|MILVUS|pymilvus|langchain-milvus|milvus_id|ENABLE_EXTRA_MCP_TOOLS" --glob '!*.pyc' --glob '!__pycache__/**' .
python -m compileall internal pkg api scripts main.py
python -m unittest tests/test_qdrant_client_wrapper.py
python -m unittest tests/test_document_management_performance.py
cd web/plantform_vue && npm run build
```

当前已验证结果：

- Milvus 关键字静态搜索无命中。
- Python compileall 通过。
- Qdrant wrapper unittest 通过。
- 文档管理性能与正文回写 unittest 通过。
- 前端 Vite build 通过，仅有 chunk 体积提示。

## 登录与对话验证

本地验证使用账号：

```txt
admin / 123456
```

已确认：

- `/users/login` 能返回 `登录成功` 和 token。
- 前端能进入 `/chat`，顶部显示 `admin`。
- `/messages` 流式对话接口能创建会话、保存用户消息、返回 AI 回复、保存 AI 消息并发送 `done` 事件。
- `show_thinking=false` 已修正为真正的布尔值，不会再因为字符串 `"false"` 被 Python 当成真而强制输出思考、操作、观察事件。
- 前端聊天面板本身保留思考、操作、观察渲染能力，是否展示取决于前端发送的 `show_thinking`。

本地没有 Qdrant 或 MongoDB 时，不代表服务器环境不可用；服务器上的 Qdrant 和 MongoDB 按环境变量连接使用。

## 注意事项

- 不要把 `vector_id` 改回 `milvus_id`。
- 不要恢复 `internal/db/milvus.py`、`internal/db/milvus_config.py` 或 `milvus/docker-compose.yml`。
- 不要重新加 `ENABLE_EXTRA_MCP_TOOLS`，用户要求自定义工具限制已经解除。
- 如果要做真实接口复测，需要本机 MongoDB、Redis、Qdrant 服务已启动。
- `scripts/import_knowledge_snapshot.py` 当前只导入 `type=qdrant` 的快照向量；旧 Milvus 快照不作为默认兼容目标。
- `scripts/backfill_document_content.py` 只用于补齐历史文档正文；它不新增文档，也不修改 Qdrant 数据。
- 业务文案和前端页面应使用“Qdrant”或“向量库”，不要出现 Milvus。

## 文档管理性能与详情内容规则

- 上传接口不要同步解析全文。大文件解析和向量化统一交给后台处理任务。
- 后台处理成功后必须把解析后的完整正文写回 `documents.content`，详情页正文只读这个字段。
- 列表和详情的分块数量优先取 `extra_data.chunks_count`，其次取 `page`，不要为每条文档实时扫描远程 Qdrant。
- Qdrant payload 可用于检索和 3D 可视化，不作为文档详情正文的数据源。
- 历史文档如果 `content` 为空，使用 `python scripts/backfill_document_content.py` 从 `uploads/` 原文件补齐正文。

## Kafka 远程部署与连接规则

- Kafka 统一使用服务器 `8.140.245.242:9092`，本地项目不要再连接 `127.0.0.1:9092` 或 `localhost:9092`。
- 本地没有 Kafka 不代表异常；文档处理队列以服务器 Kafka 为准。
- 服务器 Kafka 使用原生安装，不使用 Docker，不依赖 ZooKeeper。
- 服务器部署为 Kafka `kafka_2.13-4.3.0` KRaft 单节点模式。
- Java 使用 `/opt/temurin-jre-17`，服务文件里显式设置 `JAVA_HOME=/opt/temurin-jre-17`。
- Kafka 安装目录为 `/opt/kafka_2.13-4.3.0`，软链为 `/opt/kafka`。
- Kafka 数据目录为 `/var/lib/kafka/kraft-combined-logs`。
- Kafka 配置文件为 `/opt/kafka/config/kraft/server.properties`。
- `listeners` 为 `PLAINTEXT://0.0.0.0:9092,CONTROLLER://127.0.0.1:9093`。
- `advertised.listeners` 为 `PLAINTEXT://8.140.245.242:9092`。
- `controller.quorum.voters` 为 `1@127.0.0.1:9093`。
- Kafka 已注册为 systemd 服务 `kafka.service`，并设置开机自启。
- Kafka 服务设置 `KAFKA_HEAP_OPTS=-Xms512m -Xmx512m`，适配当前小内存服务器。
- 当前已创建 topic：`document_embedding`、`document_delete`、`batch_processing`。
- 本地 Kafka 客户端固定 `api_version=[4, 3]`，避免启动时自动探测 metadata 出现 `NodeNotReadyError: metadata`。
- `request_timeout_ms` 可作为 Kafka 公共配置；`max_block_ms` 只放在 producer 配置里，不要传给 consumer。
- 本地 Docker 容器 `brainwave_kafka_ui`、`brainwave_kafka`、`brainwave_zookeeper` 应保持停止，并关闭自动重启。
- 如果本地连接 `8.140.245.242:9092` 失败，先检查阿里云安全组是否放行 TCP `9092`，建议只允许当前本机公网 IP 访问。
- 之前 Kafka 报错的主要原因是本地配置和客户端回退地址仍可能走本地 `localhost` / `127.0.0.1`，且本地服务状态不应作为现在的判断依据。
- 服务器直接从 Apache 下载 Kafka 如果很慢，可以使用服务器可访问的镜像下载；也可以本地下载后通过 `scp` 上传到服务器。
- 服务器安装包可用命令：`wget https://mirrors.tuna.tsinghua.edu.cn/apache/kafka/4.3.0/kafka_2.13-4.3.0.tgz -O /tmp/kafka_2.13-4.3.0.tgz`。
- 本地上传备用命令：`scp /tmp/kafka_2.13-4.3.0.tgz root@8.140.245.242:/tmp/`。
- 验证 Kafka 服务：`ssh root@8.140.245.242 'systemctl is-active kafka && ss -lntp | grep 9092'`。
- 验证本地连通：`nc -vz 8.140.245.242 9092`。

## Kafka metadata 报错排障记录

本次出现过：

```txt
kafka.errors.NodeNotReadyError: NodeNotReadyError: metadata
WARNING:kafka.cluster:No node available for metadata refresh - backoff/retry
```

这不等于远程 Kafka 已经宕机。实际排查时服务器 Kafka 是正常的：

- `systemctl is-active kafka` 返回 `active`。
- `ss -lntp | grep 9092` 能看到 Kafka 监听 `0.0.0.0:9092`。
- `nc -vz 8.140.245.242 9092` 本地能连通。
- `/opt/kafka/bin/kafka-broker-api-versions.sh --bootstrap-server 127.0.0.1:9092` 能返回 `Metadata(3)` 等 API。
- `/opt/kafka/bin/kafka-topics.sh --bootstrap-server 127.0.0.1:9092 --list` 能看到业务 topic。

真正的问题在客户端侧：

- `kafka-python` 连接 Kafka `4.3.0` 时，如果不固定 `api_version`，启动阶段会自动探测 broker API。
- 自动探测期间可能出现 metadata 刷新时机不稳定，日志里会看到 `NodeNotReadyError: metadata`。
- 端口能通只能证明 TCP 通，不代表客户端 metadata 协商稳定。
- 因此本项目固定 `api_version = [4, 3]`，让 producer 和 consumer 都跳过自动探测。

本次还踩过一个配置边界：

- `request_timeout_ms` 是 producer 和 consumer 都支持的公共配置，可以放在 `[kafka]`。
- `max_block_ms` 只支持 producer，不能从公共配置传给 consumer。
- 如果把 `max_block_ms` 传给 consumer，会报 `KafkaConfigurationError: Unrecognized configs: {'max_block_ms'}`。
- 所以 `max_block_ms` 必须只放在 `[kafka.producer]`。

以后遇到 Kafka 报错按这个顺序查：

1. 先确认服务器没有宕：`ssh root@8.140.245.242 'systemctl is-active kafka && ss -lntp | grep 9092'`。
2. 再确认本地网络：`nc -vz 8.140.245.242 9092`。
3. 再确认 broker metadata：`ssh root@8.140.245.242 'export JAVA_HOME=/opt/temurin-jre-17; export PATH=/opt/temurin-jre-17/bin:$PATH; /opt/kafka/bin/kafka-broker-api-versions.sh --bootstrap-server 127.0.0.1:9092 | head'`。
4. 再看本地配置是否仍是 `bootstrap_servers = ["8.140.245.242:9092"]` 和 `api_version = [4, 3]`。
5. 如果 producer 能发消息，说明 Kafka 主链路可用；不要只因为后台 metadata warning 就判断服务器坏了。

本次修复点：

- `internal/document_client/config.toml` 固定 `api_version = [4, 3]`。
- `internal/document_client/message_client.py` 把 TOML 数组 `[4, 3]` 转成 Python tuple `(4, 3)` 后传给 Kafka 客户端。
- `internal/document_client/message_client.py` 只把 `api_version`、`request_timeout_ms` 作为 producer/consumer 公共配置。
- `internal/document_client/config.toml` 把 `max_block_ms = 10000` 放在 `[kafka.producer]` 下。
- `tests/test_document_management_performance.py` 增加测试，防止以后把 producer-only 配置传给 consumer。

## Git 推送报错排障记录

本次推送 GitHub 时出现过几类错误：

```txt
Could not resolve host: github.com
Error in the HTTP2 framing layer
Failed to connect to github.com port 443
```

处理结论：

- `Could not resolve host: github.com` 是本机 DNS 解析失败，不是 Git 仓库问题。
- 当时 `/etc/hosts` 没有被成功写入，不能假设 hosts 已经修复。
- 本机 Wi-Fi DNS 从单一 `114.114.114.114` 改成 `1.1.1.1`、`8.8.8.8`、`223.5.5.5` 后，`github.com` 可以重新解析。
- `Error in the HTTP2 framing layer` 是 HTTPS/HTTP2 传输层问题，代码和提交本身没坏。
- `Failed to connect to github.com port 443` 可能是当前解析到的 GitHub IP 不通，需要换一个可连的 GitHub IP。

以后 Git 推送失败按这个顺序查：

1. 先看 Git 状态：`git status --short --branch`。
2. 查 DNS：`networksetup -getdnsservers Wi-Fi`。
3. 查 GitHub 解析：`dscacheutil -q host -a name github.com`。
4. 查 443 端口：`nc -vz github.com 443`。
5. 如果 DNS 不稳定，设置 Wi-Fi DNS：`networksetup -setdnsservers Wi-Fi 1.1.1.1 8.8.8.8 223.5.5.5`。
6. 刷新 DNS 缓存：`dscacheutil -flushcache && killall -HUP mDNSResponder`。
7. 如果 HTTP/2 报错，使用 HTTP/1.1 推送：`git -c http.version=HTTP/1.1 push origin main`。
8. 如果 `github.com` 当前 IP 端口不通，先查可用 IP：`dig +short @1.1.1.1 github.com A`。
9. 找到可连 IP 后临时指定本次 Git 请求，不要长期写死 hosts：`git -c http.version=HTTP/1.1 -c http.curloptResolve=github.com:443:20.205.243.166 push origin main`。

本次可用经验：

- `20.200.245.247` 当时能解析出来，但连接 `github.com:443` 超时。
- `20.205.243.166` 当时可以连通 `443`，用 `http.curloptResolve=github.com:443:20.205.243.166` 后推送成功。
- 推送成功后要跑 `git fetch origin main && git status --short --branch`，确认本地 `main` 和 `origin/main` 已同步。
- 不要因为第一次 push 失败就重复提交；先确认本地提交是否已经存在，再只重试 push。

## 后续交接记录固定规则

以后本项目所有交接内容都写在 `docs/交接文档.md`，不要再新增其他交接文件。

需要写入本文件的内容包括：

- 新出现的线上或本地报错。
- 报错时的关键日志和堆栈。
- 判断是否为服务器问题、本地环境问题、代码问题、配置问题的依据。
- 根因是什么，为什么会坏。
- 修改了哪些文件，为什么这样改。
- 哪些配置不能混用，哪些配置只能放在特定位置。
- 执行过哪些验证命令，结果是什么。
- 如果推送、部署、启动失败，失败原因和可复用处理命令。
- 还没有解决的问题、临时绕过方式、后续接手需要注意的风险。

每次写交接时按这个格式追加：

```md
## YYYY-MM-DD 问题标题

### 现象

- 写用户看到的现象或命令报错。

### 原因

- 写最终确认的根因。

### 修改

- 写改动文件和改动目的。

### 验证

- 写实际跑过的命令和结果。

### 后续注意

- 写下次遇到同类问题怎么判断、怎么处理。
```

交接文档只写给后续接手人员看，不要把这些开发说明写到用户页面、接口返回文案或业务 UI 里。

## 2026-06-23 Kafka 仍连接 localhost 的报错

### 现象

日志里出现：

```txt
WARNING:kafka.net.inet:DNS lookup failed for localhost:9092
ERROR:kafka.net.manager:Connection failed: KafkaConnectionError: DNS Resolution failure
ERROR:kafka.cluster:Metadata refresh: failed KafkaConnectionError: DNS Resolution failure
kafka.errors.NodeNotReadyError: NodeNotReadyError: metadata
```

### 原因

- 这次日志的关键点是 `localhost:9092`，不是 `8.140.245.242:9092`。
- 当前仓库 `internal/document_client/config.toml` 已经是 `bootstrap_servers = ["8.140.245.242:9092"]`。
- 当前代码 `internal/document_client/message_client.py` 的兜底地址也已经是 `8.140.245.242:9092`。
- 因此如果运行日志还出现 `localhost:9092`，说明当前正在跑的后端进程没有加载最新代码或最新配置。
- 常见原因是后端进程在修改配置前已经启动，Python 进程内的 Kafka 单例已经按旧配置初始化；只改文件不会自动刷新这个旧进程。

### 修改

- 代码无需再改 Kafka 地址；配置已经是远程 Kafka。
- 处理方式是停止旧后端进程，重新启动后端，让进程重新读取 `internal/document_client/config.toml`。

### 验证

先确认当前文件配置：

```bash
rg -n 'bootstrap_servers|localhost:9092|127\.0\.0\.1:9092' internal docs --glob '!*.pyc'
```

确认远程 Kafka：

```bash
nc -vz 8.140.245.242 9092
ssh root@8.140.245.242 'systemctl is-active kafka'
```

确认当前运行进程：

```bash
ps aux | rg 'uvicorn|main.py|plantform|python' | rg -v 'rg '
```

### 后续注意

- 看到 `localhost:9092` 时，优先判断为旧进程或旧配置，不要先怀疑远程 Kafka 宕机。
- Kafka 配置变更后必须重启后端；只保存文件不够。
- 如果同时存在多个后端进程，例如 `uvicorn main:app --port 8001` 和 `python main.py`，要确认前端实际请求的是哪个端口。
- 重启后日志中应出现 `Kafka 客户端已初始化，服务器: ['8.140.245.242:9092']`。

## 2026-06-24 向量检索返回高度重复文本块

### 现象

- 知识库检索返回的多个文本块内容高度相似，尤其是同一个 docx 的不同 chunk。
- 实测查询 `RAG 平台 知识库 检索` 时，Qdrant 原始前 10 条结果里最高文本相似度达到 `1.0`。

### 原因

- 向量库里存在大量模板化、重复度很高的文本块，同一个文档不同位置的 chunk 只有编号或少量文字不同。
- 原来的 `RAGService._deduplicate_results()` 主要按分数接近过滤，不按文本内容相似度过滤；相似文本只要分数差异超过阈值就会继续返回。
- MCP 版知识库工具原来直接查 Qdrant，没有走 `RAGService.search()`，因此也绕过了 RAG 层的去重和 rerank。

### 修改

- 新增 `internal/rag/dedup.py`，把文本归一化、相似度计算和检索结果去重拆成独立模块。
- `internal/rag/rag_service.py` 统一调用新的去重模块，按文本相似度过滤高度重复文本块。
- `pkg/agent_tools_mcp/knowledge_search_mcp.py` 改为走 `RAGService.search()`，让 MCP 工具和普通知识库工具使用同一套去重逻辑。
- `pkg/agent_tools/knowledge_search.py` 清理终端过程输出，避免工具执行过程混入用户可见内容。
- `tests/test_qdrant_client_wrapper.py` 增加回归测试，覆盖相似文本块去重、不同文本块保留、MCP 入口走统一检索路径。

### 验证

已执行：

```bash
python -m unittest tests/test_qdrant_client_wrapper.py
python -m compileall internal pkg api scripts main.py
```

结果均通过。

真实 Qdrant 查询验证：

```txt
query= RAG 平台 知识库 检索
raw_count= 10 raw_max_similarity= 1.0
dedup_count= 3 dedup_max_similarity= 0.8318
```

说明原始召回里确实存在高度重复文本块，修复后返回结果的最高相似度已降到去重阈值以下。

### 后续注意

- 如果后续仍觉得检索内容重复，先区分是 Qdrant 原始召回重复、RAG 去重失效，还是 agent 多次调用工具导致上下文重复。
- MCP 知识库检索不要再直接调用 `qdrant_client.search_documents()`，否则会绕过统一去重逻辑。
- 不要只按分数接近去重；分数接近不代表文本重复，容易误删不同内容。

## 2026-06-24 远程 MongoDB 加认证并迁移本地数据

### 现象

- 本地 `.env` 原来连接 `127.0.0.1:27017`，本地 Mongo 容器停止时会连不上。
- 改连 `8.140.245.242:27017` 后，远程 Mongo 能 ping 通，但最初没有 `rag_platform` 业务数据。
- 远程 Mongo 原来没有开启认证，不带账号也可以连接并列出数据库。

### 原因

- 本地业务数据在 Docker 容器 `brainwave_mongodb` 的 volume 中。
- 远程 Mongo 使用 systemd 运行，配置文件为 `/etc/mongod-rag.conf`，数据目录为 `/var/lib/mongodb`。
- 远程 Mongo 原配置没有 `security.authorization`，因此需要先创建用户，再开启认证。

### 修改

- 启动本地 `brainwave_mongodb`，从本地 `rag_platform` 导出数据。
- 上传 dump 到远程服务器并恢复到远程 Mongo 的 `rag_platform`。
- 远程创建：
  - 管理用户：`root`
  - 项目用户：`rag_user`
- 修改远程 `/etc/mongod-rag.conf`，追加：

```yaml
security:
  authorization: enabled
```

- 重启远程 `mongod.service`。
- 本地 `.env` 改为远程项目用户连接串：

```env
MONGODB_URL=mongodb://rag_user:rag_password_2026@8.140.245.242:27017/rag_platform?authSource=rag_platform
MONGODB_DATABASE=rag_platform
```

### 验证

远程 Mongo 服务：

```bash
ssh root@8.140.245.242 'systemctl is-active mongod && ss -lntp | grep 27017'
```

本地 dump：

```txt
rag_platform 导出 9 个集合，共 105 条记录。
```

远程恢复后集合数量：

```txt
documents 8
user_info 6
chunks 12
thought_chains 18
message 40
session 10
qa_caches 4
evaluations 4
benchmarks 3
```

认证验证：

```txt
无密码：ping 成功，但读取业务集合返回 Unauthorized
项目用户：ping 成功，documents_count=8
root 用户：ping 成功，documents_count=8
```

项目 `.env` 验证：

```txt
MONGODB_URL=mongodb://rag_user:rag_password_2026@8.140.245.242:27017/rag_platform?authSource=rag_platform
documents_count=8
collections=['benchmarks', 'chunks', 'documents', 'evaluations', 'message', 'qa_caches', 'session', 'thought_chains', 'user_info']
```

### 后续注意

- 远程 Mongo 现在需要账号才能读取业务数据。
- 项目默认使用 `rag_user`，不要在业务配置里使用 root。
- 远程 `/var/lib/mongodb` 已在迁移前备份到 `/root/mongo-backups/mongodb-before-rag-20260624101859.tgz`。
- 如果后端已经启动，修改 `.env` 后必须重启后端进程，旧进程不会自动读取新的 MongoDB 地址。

## 2026-06-24 文档上传去重与语义完整性

### 现象

- 相同文件重复上传会生成新的 Mongo 文档和新的 Qdrant 向量。
- 单个文档内部存在完全相同的文本块时，原流程会重复 embedding 并写入向量库。
- 只在检索阶段去重可以减少返回重复，但不能减少入库存储和处理耗时。

### 原因

- 上传入口原来只按 UUID 生成新文件名，没有计算文件内容哈希。
- 文档处理链路原来是全文提取后直接分块、embedding、写 Qdrant，没有入库前的精确内容去重。
- `document_uuid + chunk_index` 只能保证同一文档同一序号重复处理时覆盖，不能阻止同一文件重复上传，也不能阻止同一文档内部不同 chunk 内容相同。

### 修改

- 上传前计算文件内容 `sha256`。
- 新文档在 `extra_data` 中保存 `file_sha256`、`original_filename`、`dedup_status`。
- 上传时按 `file_sha256 + permission` 查找处理中或处理完成的已有文档；命中时直接返回已有文档，不保存新文件、不创建新 Mongo 记录、不提交处理任务。
- 文档处理时在 embedding 前做精确 chunk 去重，只合并归一化空白后完全相同的 chunk，不做相似去重。
- 处理结果写回 `raw_chunks_count`、`deduped_chunks_count`、`duplicate_chunks_removed`、`vectors_count` 和 `chunks_count`。
- Mongo `documents.content` 仍保存完整原文，去重只影响 Qdrant 入库。

### 验证

已执行：

```bash
python -m unittest tests/test_document_upload_deduplication.py
python -m unittest tests/test_document_management_performance.py
python -m unittest tests/test_qdrant_client_wrapper.py
python -m compileall internal pkg api scripts main.py
```

指定文件真实验证：

```txt
测试文件：/Users/haogeng/Desktop/文档/开发笔记/1.开发笔记.md
raw_chunks_count=461
deduped_chunks_count=455
duplicate_chunks_removed=6
content_length=173569
```

二次上传/查重验证：

```txt
after_first_docs=9
after_first_vectors=2090
after_duplicate_docs=9
after_duplicate_vectors=2090
duplicate_lookup_uuid=44146f25-a517-47f5-be2e-5577f60986ff
```

语义召回验证：

```txt
Django 创建项目：命中
JWT token 配置：命中
DRF 配置：命中
跨域请求：命中
富文本编辑器：命中
```

真实验证结束后已清理测试文档和向量：

```txt
final_docs=8
final_vectors=1635
```

### 后续注意

- 文件级去重以 `file_sha256 + permission` 为边界，不同权限的相同文件允许各自存在，避免权限串数据。
- 内容级去重只做精确去重，不要改成相似去重，否则容易误删教程步骤、编号相近段落和代码块。
- 如果后续要对历史文档补做入库去重，需要先备份 Mongo 和 Qdrant，再按文档重建向量；不要直接删除向量点。

## 2026-06-24 RAG 调用评估记录和管理中心展示

### 现象

- 原来只做了基准集离线评估和 JSON 报告，不符合真实使用场景。
- 需要在用户真实问答中，只记录实际调用 RAG 的问题、返回文本、检索分数和 RAGAS 分数。
- 管理员需要在管理中心直接查看这些记录，不再去本地找 JSON 文件。

### 原因

- 真实问答没有人工标准答案，不能把生产链路里的 `context_recall` 当成严格召回率强行计算。
- 生产链路更需要看到每次 RAG 实际返回了哪块文本，以及该片段的向量分数、重排分数和 RAGAS 补充评估结果。
- RAGAS 可能较慢或失败，不能阻塞用户正常收到回答。

### 修改

- `evaluations` 集合继续复用 `EvaluationModel`，新增问题、回答、会话、用户、消息、文档、返回文本、检索分数、RAGAS 状态等字段。
- 只有 `knowledge_search` 实际返回 RAG 片段时才写入评估记录；普通闲聊不写入。
- AI 回答保存后先写入检索记录，再异步执行 RAGAS，回填 `faithfulness`、`answer_relevance`、`context_precision`。
- `overall_score` 的口径是优先取 `rerank_score`，没有重排分数时取 `vector_score`。
- 生产问答没有标准答案时，`context_recall` 不强行计算，保留为 `0.0`。
- 新增后台接口 `GET /evaluations/rag`，支持分页、关键词和 RAGAS 状态筛选。
- 管理中心新增 `RAG 评估` 页面，展示问题、返回文本、文档来源、检索分数、RAGAS 分数、评估状态和时间。
- 删除离线 JSON 报告脚本，当前目标是数据库记录和管理中心展示。

### 验证

建议验证：

```bash
python3 -m unittest tests.test_rag_evaluation_records -v
python3 -m py_compile internal/service/evaluation/rag_evaluation_service.py api/v1/evaluation_controller.py
cd web/plantform_vue && npm run build
```

真实链路验证：

1. 登录系统并提一个会调用知识库的问题。
2. 等 AI 回答保存完成。
3. 进入管理中心的 `RAG 评估`。
4. 确认能看到该问题、返回文本、文档来源、检索分数和 RAGAS 状态。
5. 普通闲聊不应该出现在该页面。

### 后续注意

- RAGAS 失败只影响该条记录的评估状态，不影响用户聊天。
- 如果要严格计算 `context_recall`，必须使用带标准答案的基准集或人工标注样例，不能在普通生产问答里伪造。
- 用户页面不要展示实现说明、接口说明或调试语句；这些内容只保留在交接文档里。

## 2026-06-24 RAGAS 默认进入 Kafka 队列

### 现象

- 直接对每个 RAG 片段 `create_task` 跑 RAGAS 不适合长期运行，无法在管理中心看到等待中的任务总数和每条任务状态。
- 管理员需要默认把评估任务放进 Kafka，并在管理中心看到排队、评估中、完成、失败和跳过状态。

### 原因

- RAGAS 评估耗时不可控，不能绑定在聊天链路里等待。
- 评估任务需要可排队、可重试、可统计，否则只能看到最终分数，看不到队列积压。
- 现有 Kafka 已经作为后台任务通道，适合承接 RAGAS 评估。

### 修改

- 新增 Kafka topic：`rag_evaluation`。
- RAG 调用记录保存后，默认加入 Kafka 评估队列。
- 队列状态使用 `queue_status`：`queued`、`running`、`completed`、`failed`、`skipped`。
- 新增入队时间、开始时间、完成时间和重试次数。
- 管理中心 `RAG 评估` 页面展示总等待数、评估中、失败、已跳过、平均检索分和平均 RAGAS 分。
- 管理中心支持配置 RAGAS 开关、Kafka 入队开关、抽样比例、每题评估片段数和最低检索分。
- 支持单条记录重新加入队列。

### 验证

建议验证：

```bash
python3 -m unittest tests.test_rag_evaluation_records -v
python3 -m py_compile main.py internal/service/evaluation/rag_evaluation_service.py internal/service/evaluation/rag_evaluation_consumer.py api/v1/evaluation_controller.py
cd web/plantform_vue && npm run build
```

真实链路验证：

1. 启动后端，确认 `rag_evaluation` 消费者启动。
2. 提一个会调用知识库的问题。
3. 进入管理中心 `RAG 评估`。
4. 确认总等待数会按 `queued` 记录变化。
5. 等消费者处理后确认状态从 `queued` 到 `running` 再到 `completed` 或 `failed`。

### 后续注意

- Kafka 不可用时不能影响聊天，只允许该条评估记录进入失败状态。
- 如果服务器 Kafka topic 不存在，需要先创建或确认 Kafka 自动建 topic 策略。
- 管理中心里的等待数以数据库 `queue_status = queued` 为准，不直接读 Kafka lag。
